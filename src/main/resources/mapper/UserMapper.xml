<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTDMapper3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gg.duonduo.repository.UserRepository">

    <insert id="insertUser" parameterType="com.gg.duonduo.domain.User">
        INSERT INTO user(username, password, firstName, lastName, age, salary)
        VALUES(#{username}, #{password}, #{firstName}, #{lastName}, #{age}, #{salary})
    </insert>

    <select id="userList" resultType="com.gg.duonduo.domain.User">
        SELECT * FROM user
        ORDER BY id
    </select>

    <select id="fetchUserByID" resultType="com.gg.duonduo.domain.User"
            parameterType="int">
        SELECT * FROM user
        WHERE id = #{id}
    </select>

    <delete id="deleteUser" parameterType="int">
        DELETE FROM user
        WHERE id = #{id}
    </delete>

    <update id="updateUser" parameterType="com.gg.duonduo.domain.User">
        UPDATE user
        SET firstName = #{firstName}, lastName = #{lastName}, age = #{age}, salary = #{salary}
        WHERE id = #{id}
    </update>

</mapper>
